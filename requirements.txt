# Core (necessário fora do Databricks)
pyspark>=3.4,<4.0
delta-spark>=3.0,<4.0

# Utilitários
python-dotenv>=1.0,<2.0

# Qualidade / testes (opcional, mas recomendado)
pytest>=7.0,<9.0
pytest-cov>=4.0,<6.0

# Estilo / lint (opcional)
black>=23.0,<25.0
flake8>=6.0,<8.0
